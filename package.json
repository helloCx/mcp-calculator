{
  "name": "joey-calculator-mcp",
  "version": "1.0.1",
  "description": "A calculator MCP server — add, subtract, multiply, divide, power, sqrt, modulo",
  "type": "commonjs",
  "main": "dist/index.js",
  "bin": {
    "joey-calculator-mcp": "./dist/index.js"
  },
  "scripts": {
    "build": "esbuild index.js --bundle --platform=node --format=cjs --outfile=dist/index.js --banner:js='#!/usr/bin/env node'",
    "start": "node dist/index.js"
  },
  "files": [
    "dist/index.js"
  ],
  "keywords": [
    "mcp",
    "calculator",
    "model-context-protocol"
  ],
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.0"
  }
}
